1.仔细print(f"")的用法，f写在""的外面
2.注意调用的是变量还是函数（要不要加上括号）
3.大模型应用开发是的概念：运用大模型去开发企业需要的应用，大模型应用开发的核心工作是把大模型的能力和具体业务、数据、工具连接起来，让它能解决实际问题。
4.RAG是为了解决大模型回答用户问题时仅凭记忆回答的毛病，让大模型基于已有的文档资料来作答，RAG 的完整流程是“先检索、再生成”——先从文档库里找到相关资料，再让大模型基于资料回答。